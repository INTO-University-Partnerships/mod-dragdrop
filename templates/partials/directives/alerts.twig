<div class="alerts">
    <div class="alert" ng-class="{'alert-success': success, 'alert-error': error, 'alert-warning': warning}" ng-show="msg">
        <button type="button" class="close">&times;</button>
        <span ng-bind="msg"></span>
    </div>
</div>
